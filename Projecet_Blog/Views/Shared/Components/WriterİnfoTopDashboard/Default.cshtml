
@using Entity_Layer.Concrete
@model List<Writer>

@foreach (var item in Model)
{
    <li class="nav-item nav-profile dropdown">
        <a class="nav-link dropdown-toggle" id="profileDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
            <div class="nav-profile-img">
                <img src="@item.WriterImage" alt="image">
                <span class="availability-status online"></span>
            </div>
            <div class="nav-profile-text">
                <p class="mb-1 text-black">@item.WriterName</p>
            </div>
        </a>

        <div class="dropdown-menu navbar-dropdown" aria-labelledby="profileDropdown">
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/Login/LogOut">
                <i class="mdi mdi-logout mr-2 text-primary"></i> Çıkış Yap
            </a>
            <a class="dropdown-item" href="/Admin/Widget/Index">
                <i class="mdi mdi-account-star mr-2 text-primary"></i> Admin Paneli
            </a>
        </div>
    </li>
}

<li class="nav-item d-none d-lg-block full-screen-link">
    <a class="nav-link">
        <i class="mdi mdi-fullscreen" id="fullscreen-button"></i>
    </a>
</li>
